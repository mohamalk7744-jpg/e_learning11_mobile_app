# ⚡ البدء السريع - تطوير محلي

## 3 خطوات فقط للبدء:

### 1️⃣ تثبيت المكتبات
```bash
cd e_learning_mobile_app
pnpm install
```

### 2️⃣ تشغيل الخادم (في Terminal منفصل)
```bash
pnpm dev:server
```

### 3️⃣ تشغيل التطبيق
```bash
pnpm dev
```

---

## 📱 على الهاتف:

1. حمّل **Expo Go** من App Store أو Google Play
2. امسح **QR Code** الذي يظهر في Terminal
3. التطبيق سيفتح على هاتفك! 🎉

---

## 🌐 على المتصفح:

اضغط `w` في Terminal عند ظهور الخيارات

---

## 📝 بيانات الدخول:
- **البريد:** student@example.com
- **كلمة المرور:** password123

---

## 🔥 تعديل الكود:

أي تغيير تجريه في الملفات سيظهر فوراً على الهاتف/المتصفح! ✨

---

## 📚 ملفات مهمة:

| الملف | الوصف |
|------|-------|
| `app/(tabs)/` | الشاشات الرئيسية |
| `server/routers.ts` | APIs |
| `components/` | المكونات |
| `constants/theme.ts` | الألوان والأنماط |

---

## ❓ مشاكل شائعة:

**الهاتف لا يتصل؟**
- تأكد من نفس الشبكة WiFi
- أعد تشغيل Expo Go

**QR Code لا يظهر؟**
```bash
pnpm dev
```

**خطأ في المكتبات؟**
```bash
rm -rf node_modules
pnpm install
```

---

## 🎯 الخطوة التالية:

اقرأ `SETUP_LOCAL.md` للتفاصيل الكاملة
