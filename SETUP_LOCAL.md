# 🚀 دليل تشغيل تطبيق التعلم عن بعد محلياً على جهازك

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

### 1. **Node.js و npm** (أو pnpm)
- اذهب إلى: https://nodejs.org
- حمّل النسخة LTS (الإصدار المستقر)
- تحقق من التثبيت:
```bash
node --version
npm --version
```

### 2. **Git** (لاستنساخ المشروع)
- اذهب إلى: https://git-scm.com
- حمّل وثبّت Git
- تحقق من التثبيت:
```bash
git --version
```

### 3. **Expo CLI** (لتشغيل التطبيق)
```bash
npm install -g expo-cli
```

---

## خطوات التشغيل

### الخطوة 1: استنساخ المشروع
```bash
# انسخ المشروع من المستودع
git clone <رابط المستودع>
cd e_learning_mobile_app
```

### الخطوة 2: تثبيت المكتبات
```bash
# استخدم pnpm (الأسرع)
pnpm install

# أو استخدم npm
npm install
```

### الخطوة 3: إعداد متغيرات البيئة
أنشئ ملف `.env.local` في جذر المشروع:
```bash
# في جذر المشروع
echo "GEMINI_API_KEY=your_gemini_api_key_here" > .env.local
```

استبدل `your_gemini_api_key_here` بمفتاح Gemini API الفعلي.

### الخطوة 4: تشغيل الخادم (Backend)
```bash
# في نافذة Terminal منفصلة
pnpm dev:server
```

سيبدأ الخادم على `http://localhost:3000`

### الخطوة 5: تشغيل التطبيق (Frontend)
```bash
# في نافذة Terminal أخرى
pnpm dev
```

سيظهر QR Code في Terminal. اختر:
- **للويب**: اضغط `w`
- **لـ iOS**: اضغط `i`
- **لـ Android**: اضغط `a`

---

## 📱 تشغيل على الهاتف الفعلي

### على iPhone (iOS):
1. حمّل تطبيق **Expo Go** من App Store
2. افتح التطبيق
3. امسح QR Code الذي يظهر في Terminal
4. سيفتح التطبيق على هاتفك مباشرة

### على Android:
1. حمّل تطبيق **Expo Go** من Google Play Store
2. افتح التطبيق
3. امسح QR Code الذي يظهر في Terminal
4. سيفتح التطبيق على هاتفك مباشرة

---

## 🌐 تشغيل على المتصفح (الويب)

```bash
pnpm dev
# ثم اضغط w للويب
```

سيفتح التطبيق على `http://localhost:8081`

---

## 📝 بيانات تسجيل الدخول التجريبية

```
البريد الإلكتروني: student@example.com
كلمة المرور: password123
```

---

## 🔧 الأوامر المهمة

```bash
# تشغيل التطبيق في وضع التطوير
pnpm dev

# تشغيل الخادم
pnpm dev:server

# بناء التطبيق للإنتاج
pnpm build

# تنظيف الملفات المؤقتة
pnpm clean

# فحص الأخطاء
pnpm type-check

# تشغيل الاختبارات
pnpm test
```

---

## 🐛 استكشاف الأخطاء الشائعة

### المشكلة: QR Code لا يظهر
**الحل:**
```bash
# أعد تشغيل الخادم
pnpm dev
```

### المشكلة: "Cannot find module"
**الحل:**
```bash
# أعد تثبيت المكتبات
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

### المشكلة: الخادم لا يعمل
**الحل:**
```bash
# تأكد من أن المنفذ 3000 مجاني
# أو غيّر المنفذ في ملف الإعدادات
```

### المشكلة: الهاتف لا يتصل بالخادم
**الحل:**
1. تأكد من أن الهاتف والكمبيوتر على نفس الشبكة WiFi
2. تحقق من عنوان IP للكمبيوتر:
   - **Windows**: `ipconfig`
   - **Mac/Linux**: `ifconfig`
3. حدّث عنوان IP في إعدادات الاتصال

---

## 📂 هيكل المشروع

```
e_learning_mobile_app/
├── app/                    # شاشات التطبيق
│   ├── (tabs)/            # التبويبات الرئيسية
│   ├── auth/              # شاشات المصادقة
│   └── _layout.tsx        # التخطيط الرئيسي
├── server/                # كود الخادم
│   ├── routers.ts         # APIs
│   ├── services/          # الخدمات (Gemini, إلخ)
│   └── db.ts              # دوال قاعدة البيانات
├── components/            # مكونات قابلة لإعادة الاستخدام
├── hooks/                 # React Hooks مخصصة
├── lib/                   # مكتبات مساعدة
├── constants/             # الثوابت والإعدادات
├── drizzle/               # قاعدة البيانات
└── assets/                # الصور والأيقونات
```

---

## 🔐 إضافة مفتاح Gemini API

1. اذهب إلى: https://ai.google.dev
2. انقر على "Get API Key"
3. اختر مشروع Google Cloud أو أنشئ واحداً جديداً
4. انسخ المفتاح
5. أضفه في ملف `.env.local`:
```bash
GEMINI_API_KEY=your_key_here
```

---

## 📚 موارد إضافية

- **Expo Documentation**: https://docs.expo.dev
- **React Native**: https://reactnative.dev
- **Drizzle ORM**: https://orm.drizzle.team
- **tRPC**: https://trpc.io

---

## ❓ أسئلة شائعة

**س: هل يمكن تطوير التطبيق بدون Expo Go؟**
ج: نعم، يمكنك استخدام محاكي iOS/Android من Xcode أو Android Studio.

**س: كيف أضيف ميزة جديدة؟**
ج: 
1. أنشئ ملف جديد في `app/` أو `components/`
2. أضف API جديد في `server/routers.ts`
3. اختبر التغييرات على الفور

**س: كيف أنشر التطبيق؟**
ج: استخدم Expo Application Services (EAS) أو بناء APK/IPA يدويّاً.

---

## 🎯 الخطوات التالية

1. ✅ شغّل التطبيق محلياً
2. ✅ اختبر جميع الميزات
3. ✅ أضف ميزات جديدة
4. ✅ اختبر على أجهزة حقيقية
5. ✅ انشر التطبيق

---

**تم إنشاء هذا الدليل في:** 2026-01-07
**آخر تحديث:** 2026-01-07
